# FastAPI
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# 配置
python-dotenv>=1.0.0
pydantic-settings>=2.0.0

# LangGraph + LangChain
langgraph>=0.0.26
langchain-core>=0.1.0
langchain-community>=0.0.20
langchain-openai>=0.0.5
openai>=1.0.0

# 向量检索（对齐 DB-GPT 的 DBSummary 思路）
chromadb>=0.5.0

# 数据库只读（业务查数）
pymysql>=1.0.0
